import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // TODO: finalize bundle ID before store submission
  appId: 'com.placeholder.dominoproanota',
  appName: 'Domino Pro Anota',
  webDir: 'www',
};

export default config;
