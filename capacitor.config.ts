import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // TODO: finalize bundle ID before store submission
  appId: 'com.placeholder.dominoproscoreboard',
  appName: 'Domino Pro Scoreboard',
  webDir: 'www',
};

export default config;
